machine:
  python:
    version: 2.7.6

test:
  override:
    - sh run_all_tests.sh
    
  post:
    - mkdir $CIRCLE_ARTIFACTS/htmlcov
    - mv htmlcov/* $CIRCLE_ARTIFACTS/htmlcov
    - mv nose2-junit.xml $CIRCLE_ARTIFACTS
    